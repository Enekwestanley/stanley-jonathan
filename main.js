const typed = new Typed('.typed', {
    strings: ['Stanley & Jonathan'],
    typedSpeed: 90,
    backSpeed: 90,
    startDelay: 50,
    backDelay: 85,
    loop: true,
    showCursor: true,
    cursorChar: '|',
});